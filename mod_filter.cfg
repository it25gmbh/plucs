
%% ---------------------------------------------------------------
%% Access configurations for mod_filter.
%% You may reference any ACL entries already defined,
%% especially those in /etc/ejabberd/mod_filter_groups.cfg
%%
%% Rules won't be effective immediately, you have to restart the service,
%% or issue 'ejabberd_config:load_file("/home/ejabberd/ejabberd.cfg").'
%% on the Erlang console of the node.
%% ---------------------------------------------------------------

{access, mod_filter, [{allow, all}]}.
{access, mod_filter_presence, [{allow, all}]}.
{access, mod_filter_message, [{allow, all}]}.
{access, mod_filter_iq, [{allow, all}]}.
